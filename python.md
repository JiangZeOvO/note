# Python 相关知识笔记

## 1. 概述

### 1.1 语言特性

Python 是一种面向对象编程,解释型,强类型的动态脚本语言,具有跨平台兼容性好等特点

**解释型** 通过解释器将编写的代码解析成机器码,仅在执行到指定代码时进行动态解析该行代码,这种特性使得 Python 跨平台兼容性好

**编译型** 与之相对的编译型语言通过编译器将编写的代码编译成机器码,在执行前将全部代码一次性编译完毕,一般情况下速度快过解释型语言,不过目前部分解释型语言的动态优化过于完善,以至于某些解释型语言性能超过编译型语言

### 1.2 安装与命令

**安装** 可以通过[Python 官方网站](https://www.python.org/)下载 Python 解释器进行安装

| 命令                     | 参数             | 含义             | 注释              |
|------------------------|----------------|----------------|-----------------|
| `python`               |                | 进入 Python 交互模式 |                 |
| `exit()`               |                | 退出 Python 交互模式 | 也可使用 `Ctrl + Z` |
| `where python`         |                | 查询 Python 安装路径 |                 |
| `pip install [module]` | `[module]` 为库名 | 下载第三方 Python 库 |                 |

## 2. 数据类型

### 2.1 标量类型

#### 2.1.1 整形[`int`]

```python
var = 5
```

#### 2.1.2 浮点型[`float`]

```python
var = 5.0
var = 5.0E+0
```

#### 2.1.3 复数[`complex`]

复数的定义方式为 $a + bi$ ,其中 $a$ 为实部, $b$ 为虚部, $i$ 为虚数单位(在 Python 中用 $j$ 表示)

```python
var = 1 + 2j
```

##### 方法

##### 注释

**共轭** 复数的共轭为实部相同,虚部互为相反数,例如 $1 + 2$ 的共轭结果为 $1 - 2j$

**取模** $1 + 2j$ 取模结果为 $\sqrt[]{1^2+2^2}$

#### 2.1.4 布尔型[`bool`]

```python
var = True
var = False
```

#### 2.1.5 字符串[`str`]

```python
var = 'test'
var = "test"
var = """test"""
```

## 3. 函数

### 3.1 数学函数

`abs` 绝对值

| 参数     |          类型           | 范围 | 默认值 | 注释        |
| :------- | :--------: | :--- | :----: | :--------------------------------------------------------- |
| `x`      | `int` `float` `complex` |      |        | 数字        |
| `return` | `int` `float` `complex` |      |        | `x`的绝对值 |

`cmd` 比较 ==Python 3 已弃用==

| 参数     |     类型      | 范围                                                  | 默认值 | 注释 |
| :------- | :--------: | :--- | :----: | :--------------------------------------------------------- |
| `x`      | `int` `float` |                                                       |        | 数字 |
| `y`      | `int` `float` |                                                       |        | 数字 |
| `return` |     `int`     | x < y 返回 `-1`<br/>x = y 返回 `0`<br/>x > y 返回 `1` |        |      |

`max` 最大值

| 参数     |                      类型                       | 范围 | 默认值 | 注释                                                       |
| :------- | :---------------------------------------------: | :--- | :----: | :--------------------------------------------------------- |
| `x`      |                    `int...`                     |      |        | 按数字排序                                                 |
| `x`      |                   `float...`                    |      |        | 按数字排序                                                 |
| `x`      |                    `str...`                     |      |        | 按ASCII值排序                                              |
| `x`      |                    `list...`                    |      |        | 按`list`中最大值排序                                       |
| `x`      |                   `typle...`                    |      |        | 按`typle`中最大值排序                                      |
| `x`      |                    `set...`                     |      |        | 按`set`中最大值排序                                        |
| `x`      |                    `dict...`                    |      |        | 按`dict`中`key`的最大值排序                                |
| `[key]`  |                   `function`                    |      | `none` | 会把每一项`x`都传递给`[key]`函数,按照`[key]`函数返回值排序 |
| `return` | `int` `float` `str` `list` `typle` `set` `dict` |      |        | 返回最大值                                                 |

`min` 最小值

| 参数     |                      类型                       | 范围 | 默认值 | 注释                                                       |
| :------- | :---------------------------------------------: | :--- | :----: | :--------------------------------------------------------- |
| `x`      |                    `int...`                     |      |        | 按数字排序                                                 |
| `x`      |                   `float...`                    |      |        | 按数字排序                                                 |
| `x`      |                    `str...`                     |      |        | 按ASCII值排序                                              |
| `x`      |                    `list...`                    |      |        | 按`list`中最小值排序                                       |
| `x`      |                   `typle...`                    |      |        | 按`typle`中最小值排序                                      |
| `x`      |                    `set...`                     |      |        | 按`set`中最小值排序                                        |
| `x`      |                    `dict...`                    |      |        | 按`dict`中`key`的最小值排序                                |
| `[key]`  |                   `function`                    |      | `none` | 会把每一项`x`都传递给`[key]`函数,按照`[key]`函数返回值排序 |
| `return` | `int` `float` `str` `list` `typle` `set` `dict` |      |        | 返回最小值                                                 |

`round` 四舍五入

返回浮点数`number`的四舍五入值,准确的说保留值将保留到离上一位更近的一端(四舍六入),精度要求高的场景不建议使用该函数

`round(2.5)` 结果是 `2`,而不是 `3` 在 Python 中,对于小数为 `.5` 的情况,它会四舍五入到最接近的偶数

`round(3.5)` 结果是 `4`,因为 `3.5` 四舍五入到最接近的偶数是 `4`

`round(2.675, 2)` 结果是 `2.67`,而不是 `2.68`,因为 `2.675` 在二进制中无法精确表示,实际存储的值略小于 `2.675`,因此四舍五入结果为 `2.67`

| 参数        |     类型      | 范围 |      默认值       | 注释             |
| :---------- | :-----------: | :--- | :---------------: | :--------------- |
| `number`    | `int` `float` |      |                   | 数字             |
| `[ndigits]` |     `int`     |      | `none 不保留小数` | 保留小数位数     |
| `return`    |     `int`     |      |                   | 四舍五入后的数字 |

## 4. 模块

### 4.1 数学模块[ `math` ]

#### 4.1.1 属性

`pi` 圆周率

​	$\pi \approx 3.141592653589793$

`e` 自然常数

​	$e \approx 2.718281828459045$

#### 4.1.2 方法

`ceil` 向上取整

| 参数     |     类型      | 范围 | 默认值 | 注释            |
| :------- | :-----------: | :--- | :----: | :-------------- |
| `x`      | `int` `float` |      |        | 数字            |
| `return` |     `int`     |      |        | `x`向上取整的数 |

`floor` 向下取整

| 参数     |     类型      | 范围 | 默认值 | 注释            |
| :------- | :-----------: | :--- | :----: | :-------------- |
| `x`      | `int` `float` |      |        | 数字            |
| `return` |     `int`     |      |        | `x`向下取整的数 |

`round` 四舍五入

| 参数 | 类型 | 范围 | 默认值 | 注释 |
| :------- | :-----------: | :--- | :----: | :-------------- |
| `x` | `int` `float` | | | 数字 |
| `n` | `int` | | `0` | 保留小数位数 |
| `return` | `int` `float` | | | 四舍五入结果 |

`exp` 自然常数幂运算

| 参数     |     类型      | 范围 | 默认值 | 注释                   |
| :------- | :--------: | :--- | :----: | :--------------------------------------------------------- |
| `x`      | `int` `float` |      |        | 指数                   |
| `return` |     `int`     |      |        | 数学常数 $e$ 的`x`次方 |

`fabs` 绝对值

| 参数     |     类型      | 范围 | 默认值 | 注释        |
| :------- | :--------: | :--- | :----: | :--------------------------------------------------------- |
| `x`      | `int` `float` |      |        | 数字        |
| `return` |    `float`    |      |        | `x`的绝对值 |

`sqrt` 平方根

| 参数     |     类型      |     范围    | 默认值 | 注释        |
| :------- | :--------: | :--- | :----: | :--------------------------------------------------------- |
| `x`      | `int` `float` |  |        | 正数 |
| `return` | `float` |  |        | `x`的平方根 |

`pow` 幂运算

| 参数     |     类型      |     范围    | 默认值 | 注释        |
| :------- | :--------: | :--- | :----: | :--------------------------------------------------------- |
| `x`      | `int` `float` |  |        | 底数 |
| `y`      | `int` `float` |  |        | 指数 |
| `return` | `float` |  |        | `x`的`y`次方 |

